The six scheduled-task management actions now belong to the server core and are
always available. The obsolete `scheduling` extension and capability flag were
removed, old configurations that enable it are rejected, and the protocol version
is now 27.
