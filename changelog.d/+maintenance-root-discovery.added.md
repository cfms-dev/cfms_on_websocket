`maintain` now locates the nearest CFMS server runtime automatically, including
both the current `src` deployment layout and flat release layouts. Explicit file
paths remain relative to the directory where the command was invoked.
