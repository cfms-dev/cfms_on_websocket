Backup restore now consistently enforces archive and row limits, rejects unsafe
Windows paths, accepts case-insensitive SHA-256 text, and keeps deployment rollback
recoverable when release or extension copies fail.
